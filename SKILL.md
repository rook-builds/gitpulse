# gitpulse

Git repository health analyzer — commit velocity, hot files, author stats, activity patterns

## Install

```
pip install gitpulse
```

## Usage

```
gitpulse [TARGET] --limit 10 --output json
```

Formats: text, json, table, csv. Run `gitpulse introspect` for a machine-readable
description.
